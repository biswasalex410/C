#include<stdio.h>#include<stdlib.h>int main(){    int*ptr;    int n,i;    printf("Enter size of memory need:");    scanf("%d",&n);    ptr=(int*)calloc(n,sizeof(int));    if(ptr==NULL)    {        printf("Memory not allocated.\n");        exit(0);    }    else    {        printf("Memory successfully allocated using calloc.\n");        printf("Enter number of elements:");        for(i=0; i<n; ++i)        {            scanf("%d",ptr+i);        }        printf("The elements of the array are:");        for(i=0; i<n; ++i)        {            printf("%d",*ptr+i);        }        int m;        printf("\nEnter new size:");        scanf("%d",&m);        ptr=realloc(ptr,m*sizeof(int));        printf("Memory Successfully reallocated using realloc.\n");        printf("Enter new element from %d to %d:",n,m);        for(i=n; i<m; ++i)        {            scanf("%d",ptr+i);        }        printf("Total elements of the array are:");        for(i=0; i<m; ++i)        {            printf("%d,",*ptr+i);        }        free(ptr);    }    return 0;}