#include<stdio.h>#include<conio.h>#include<stdlib.h>struct student{    char id[100],name[100],phn[100];    struct node*next;}*start=NULL;void create(){    int n,i;    struct student *newnode,*current;    printf("\nEnter the num of students:");    scanf("%d",&n);    for(i=1; i<=n; i++)    {        newnode=(struct student*)malloc(sizeof(struct student));        printf("\nEnter student Name:");        scanf("%s",&newnode->name);        printf("\nEnter student ID:");        scanf("%s",&newnode->id);        printf("\nEnter student Phone Number:");        scanf("%s",&newnode->phn);        newnode->next=NULL;        if(start==NULL)        {            start=newnode;            current=newnode;        }        else        {            current->next=newnode;            current=newnode;        }    }}void display(){    struct student *current;    current=start;    printf("\n\n\nDetails of Students:\n");    while(current!=NULL)    {        printf("\n\n");        printf("%s\n%s\n%s",current->name,current->id,current->phn);        printf("\n\n");        current=current->next;    }}void finsert(){    struct student *newnode,*current;    newnode=(struct student*)malloc(sizeof(struct student));    printf("\n\n\nEnter Student Name:");    scanf("%s",&newnode->name);    printf("\nEnter student ID:");    scanf("%s",&newnode->id);    printf("\nEnter student Phone Number:");    scanf("%s",&newnode->phn);    newnode->next=NULL;    current=start;    start=newnode;    newnode->next=current;}void minsert(){    int key,pos,i;    struct student *newnode,*current,*temp,*temp1;    newnode=(struct student*)malloc(sizeof(struct student));    printf("\n\n\nEnter Student Name:");    scanf("%s",&newnode->name);    printf("\nEnter student ID:");    scanf("%s",&newnode->id);    printf("\nEnter student Phone Number:");    scanf("%s",&newnode->phn);    newnode->next=NULL;    current=start;}void linsert(){    if(start==NULL)    {        printf("\nThere is no data to delete \n");        exit(0);    }    struct student *current,*temp,*temp1;    int i,key,pos;    printf("\nEnter your data to delete\n");    scanf("%d",&key);    pos=search(key);    current=start;    for(i=1; i<=(pos-1); i++)    {        current=current->next;    }    if(start==current)    {        start=current->next;    }    else if(current->next==NULL)    {        current=start;        for(i=1; i<=(pos-2); i++)        {            current=current->next;        }        current->next=NULL;    }    else    {        temp=current;        current=start;        for(i=1; i<=(pos-2); i++)        {            current=current->next;        }        current->next=temp->next;    }    printf("\n Delete Successful\n");}void insert(){    printf("Enter your choice:");    printf("\n1. First Insert");    printf("\n2. Middle Insert");    printf("\n3. Last Insert");    printf("Enter your choice:");    int x;    while(1)    {        scanf("%d",&x);        switch(x)        {        case 1:            finsert();            break;        case 2:            minsert();            break;        case 3:            linsert();        case 4:            return menu();        }    }}void menu(){    printf("\nEnter your choice: ");    printf("\n1. Create Node.");    printf("\n2. Insert Node.");    printf("\n3. Delete Node.");    printf("\n4. Exit\n");    printf("Your choice:");}int main(){    struct student;    int choice;    while(1)    {        menu();        scanf("%d",&choice);        switch(choice)        {        case 1:            create();            display();            break;        case 2:            insert();            display();            break;        case 3:            break;        case 4:            return 0;            break;        }    }    getch();}