#include<stdio.h>#include<stdlib.h>int main(){    int*ptr;    int n,i;    printf("Enter size of Array:");    scanf("%d",&n);    ptr=(int*)calloc(n,sizeof(int));    if(ptr==NULL)    {        printf("Memory no allocated.\n");        exit(0);    }    else    {       printf("Memory Successfully allocated using callo.\n");      printf("Enter number of elements:");    for(i=0;i<n;++i)    {        scanf("%d",ptr+i);    }    printf("The elements of the array are:");    for(i=0;i<n;++i)    {        printf("%d",*ptr+i);    }}return 0;}