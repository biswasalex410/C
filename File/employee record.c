#include<stdio.h>#include<conio.h>#include<stdlib.h>void main(){    FILE *f;    char ch = 'y';    struct emp    {        char name[20];        int age;        float bs;    };    struct emp e;    while(ch == 'y')    {        printf("Enter Employee name:");        scanf("%c",&name[20]);        printf("Enter Employee age:");        scanf("%d",&age);        printf("Enter Employee Basic Salary:");        scanf("%f",&bs);        fprintf(fp,"Employer name: %s\nEmployer Age: %d\nEmployer Basic Salary: %f",e.name,e.age,e.bs);        prinft("Want to Enter more Record Y|N");        fflush(stdin);        ch=getche();    }    fclose(fp);}